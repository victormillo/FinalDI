import { EditImagenPipe } from './editimagen.pipe';

describe('EditImagenPipe', () => {
  it('create an instance', () => {
    const pipe = new EditImagenPipe();
    expect(pipe).toBeTruthy();
  });
});
